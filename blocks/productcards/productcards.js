export default async function decorate(block) {
  // Structure is already correct from EDS:
  // block > row (card) > cell (image) + cell (text)
  // No DOM restructuring needed; CSS handles the grid layout.
}
